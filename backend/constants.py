# Fixed LCA (Life Cycle Assessment) proxy values used for all sustainability
# math across the app. These are published explicitly in the UI per the
# project spec rather than computed from real LCA data.
WATER_SAVED_PER_CYCLE_LITERS = 2700
CO2_AVOIDED_PER_CYCLE_KG = 2.0
